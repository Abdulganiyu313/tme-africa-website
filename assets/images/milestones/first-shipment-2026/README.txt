Drop your photos in this folder using these filenames so I can wire
them straight into the milestone page without renaming anything:

  hero.jpg                 — best/widest shot, used as the page's main banner
  container-offload-1.jpg  — container arriving / being offloaded
  container-offload-2.jpg
  container-offload-3.jpg
  truck-loading-1.jpg      — machines being loaded onto the client's truck
  truck-loading-2.jpg
  team-1.jpg                — your team on site (optional but strong for trust)

More than 7 photos is fine — just keep numbering sequentially
(container-offload-4.jpg, etc.) and tell me how many you added.

Format: .jpg or .png, ideally under ~500KB each (matches the rest of
the site's image-optimization pass). If your phone photos are
several MB each, send them over as-is — I can recompress them with
the same process used on the machine catalog photos.
